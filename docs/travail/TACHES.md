# TÂCHES — board moteur (ouvert)

> Uniquement les tâches **ouvertes** de ce dépôt. Closes → [TACHES_archive.md](TACHES_archive.md).
> Tab5 / HA / transversal → `contexte_ia/04_Projets/TACHES.md`.
> **Git > board.** Convention : `[statut] #id — zone — prio — description — outil — dépend`

**Statuts** : ⏳ todo · 🔄 wip · 🧪 test · ✅ done · 🧊 gelé

## 📍 18/08/2026 (matin — état vérifié, pas recopié du board)

| | |
|---|---|
| ⚠️ **Deux boards ont divergé le 17/08 au soir** | Deux sessions parallèles ont écrit deux `TACHES.md` différents. Celui-ci (PR #348) avait **perdu** #T370→#T378 et #T388/#T389 ; la branche `claude/moteur-prompts-parallele-20ae55` (`ada7ab7`) les portait mais ignorait #T383/#T385. **Ce board-ci est la réconciliation des deux** : statut de chaque tâche re-vérifié contre `origin/master`, branche par branche, et non d'après les numéros #T (qui ne figurent pas toujours dans les messages de commit). |
| ✅ Prod déployée | `/healthz` → `c30e194`. Le retard annoncé la veille n'existe plus. |
| ✅ **AJEAN est joignable — le blocage de #T353 est levé** | Mesuré depuis le Deck : `192.168.1.x:8080/v1/models` → **HTTP 200**. Le réglage `--host 0.0.0.0` + pare-feu attendu d'Axel **est fait**. ⚠️ Mais `/v1/models` ne prouve pas l'inférence (piège DashScope) : la complétion réelle depuis le Deck **reste à mesurer** avant toute bascule → tâche 1 de la **23ᵉ série**. |
| 🚨 **`ollama_pc` est mort et toujours en tête de cascade** | `192.168.1.x:11434` → **000 depuis le Deck ET depuis le PC lui-même** : Ollama n'est plus lancé sur le PC, AJEAN l'a remplacé. Or `FAST_PATH_PROVIDERS` (`services/pipeline_service.py:81-87`) le garde en 2ᵉ position. La sonde d'hôte de #T360 l'écarte avant l'appel (le coût de 2,6 s est donc amorti), mais **le tier local n'a plus aucun maillon côté PC**. `ollama_local` sur le Deck (127.0.0.1:11434) répond 200 : c'est le seul local vivant → #T354 reste bloqué. |
| 🚨 CI toujours morte (#T382) | Quota Actions atteint. **La suite locale est la seule preuve.** Relancée ce matin sur la branche de la PR #348 : **1858 passés, 0 échec** ; puis sur #T371 : **1869 passés, 0 échec** (Python 3.13). |
| Livré le 18/08 | **#T371** (PR #350) — la température ne vient plus d'une autre pièce. **23ᵉ série** de délégation écrite (bascule AJEAN, mesure avant code). |
| 🎯 Prochaine action | **Déployer le Deck sur `be696ee`** (#348 mergée à 07:29 UTC ; #T385 P0 et #T383 P1 attendent dans la maison), puis merger la PR #350 (#T371) et celle-ci — `master` protégée + CI morte → override interface, **clic d'Axel**. Vérification en prod : rejouer 4 phrases réelles — un volet (#T385), un agenda **accentué** (#T383), une question d'état (#T363), une température de **buanderie** et d'**extérieur** (#T371). |

## 📍 17/08/2026 (23h30 — après la vague de merges du soir)

| | |
|---|---|
| Dépôt | `origin/master` @ `c30e194`. **Le merge GitHub est redevenu opérationnel** : les PR **#326 à #347** ont toutes été mergées entre 18h33 et 23h22, y compris les 7 branches que ce board donnait « en attente ». **0 PR ouverte**, `master` local = `origin/master`, arbre propre. |
| ⚠️ Prod | `/healthz` → `c30e194` (vérifié le 18/08 au matin) : le retard de 22 PR annoncé la veille **n'existait plus**, #T363 (P0) est en service. **Mais la PR #348 a été mergée depuis** (`be696ee`), donc le Deck est de nouveau en retard d'un cran — et ce cran porte **#T385 (P0, la commande de volet refusée à tort)** et **#T383 (P1, l'agenda sourd aux accents)**. Déploiement requis. |
| ✅ Vérification du 17/08 (23h30) | Suite complète relancée sur `c30e194` : **1839 tests collectés, 0 échec, 7 ignorés** (Python 3.12). 31 fichiers de tests ajoutés dans la journée. `tsc -b` et `vite build` au vert. **C'est la seule preuve verte de la journée** — voir la ligne CI. |
| 🚨 **CI GitHub Actions morte — #T382** | **Cause confirmée par Axel le 17/08 : la limite d'utilisation Actions est atteinte.** Les runs échouent en **4 s**, sans log récupérable (404), et même `🔍 Ruff (informatif)` tombe alors que toutes ses étapes sont `continue-on-error: true` — l'échec est avant le `checkout`, donc jamais du code. Conséquence assumée : **les 22 PR du soir ont été mergées sans preuve CI**. Tant que le quota n'est pas relevé, **la seule barrière qualité est l'exécution locale** (`pytest tests/unit` + `npm run build`) — un ✅ GitHub ne veut rien dire. |
| ⚠️ Ce que ce trou a laissé passer | **#T383** : #T365 (PR #328) est parti en prod **à moitié mort**. Six formulations accentuées réelles ne matchaient aucun marqueur, parce que `_normalize_prompt` **conserve** les accents et que les marqueurs avaient été écrits sans. Les tests de #T365 ne l'ont pas vu : ils écrivent les phrases **sans accent**. Trouvé le 17/08 à 23h50 en rejouant un banc de régression resté sur une branche de sauvegarde. |
| 🎯 Prochaine action | **Déployer le Deck.** Les deux régressions qui l'auraient pourri sont corrigées (#T383, #T385). Ordre : (1) merger la PR en cours — `master` est protégée et la CI est morte, donc **override interface obligatoire** (#T382) ; (2) `git pull` sur `origin/master` ; (3) redémarrer le service ; (4) vérifier `/healthz` → `{"commit":"…"}` (#T355) ; (5) **rejouer 3 phrases réelles** : une commande de volet (#T385), une demande d'agenda **accentuée** (#T383), une question d'état (#T363). |
| Clos le 17/08 — journée | #T343 (#301-303), #T324 (#305), #T316 (#300), #T344-a (#304), #T289 (#307), #T290 (#308), #T337 (#309), #T345 (#311), #T344 (#312), #T169 (#313), #T347 (#315), #T348 (#316), #T349 (#317), #T350 (#318), #T351 (#319), #T352 (#320), #T355 (#321), #T356 (#323) ; #T303 et #T307 **étaient déjà livrés** — voir archive |
| Clos le 17/08 — soirée | #T358 (#325), #T364 (#326), **#T363 (#327, P0)**, #T365 (#328), #T366 (#329), #T357 (#330), #T367 (#332, couvre #T359), #T362 (#333), #T360 (#324 #334), #T369 (#335), #T346 (#336), #T305-a (#337), #T353-a (#339), #T361 (#343), #T379 (#344), #T373 #T380 (#345) — voir archive |
| Mesures du 17/08 (résumé — détail en archive) | **Prod** : 943 échanges vocaux, 499 sessions — le vocal est vivant et utilisé. **Balayage** : 458 réponses rendues, 14 % problématiques ; les 3 causes classées (#T350 #T351 #T352) et les 4 défauts du rejeu de 36 phrases (#T363 #T364 #T365 #T366) sont **tous corrigés et mergés**. **Sessions zombies** : pas une panne, statut de ramassage ; seul défaut réel (#T361) corrigé. ⚠️ Le chemin `bg`/DAG (52 % de sinistre contre 7 % en `chat`) **n'a toujours pas été rejoué** — verdict non prouvé. **Coût** : les 11,28 $ du matin étaient un chiffre faux mesuré sur la base LOCALE (#T347 #T348 #T349, corrigés). |
| ⚠️ Fiabilité des mesures d'historique | **73 % des latences ne sont pas enregistrées** : sur 165 réponses réelles en 30 j, seules 45 portent une `latency_ms > 0` (120 à 0), surtout sur `discussion_chat` (94/142 muettes). Toute conclusion de performance tirée de l'historique porte donc sur **un quart des échanges**. Les mesures prises en exerçant la prod, elles, sont complètes — c'est la voie fiable. |
| ⚠️ Piège de mesure | Le trafic vocal réel est **`mode=chat` : 166 échanges sur 169 en 30 j**. `mode=ha` n'en porte que 12, et ses 4 refus `ha_mode_failure` sont des phrases STT cassées (« décembre le volet de la sœur »). **Tester en `mode=ha` ne reproduit pas l'usage** — un premier passage y avait conclu à tort que la météo était refusée. |
| Délégation | **Recompté le 18/08 dans `prompts_delegation.md` : 22 séries écrites, et les séries 17 à 22 sont livrées** (elles manquaient à ce board — voir la note des chantiers). **Un seul prompt déjà écrit n'a jamais produit de code : #T368** (banc de non-régression, 19ᵉ série). **23ᵉ série écrite le 18/08** : bascule AJEAN, en deux temps — mesurer une complétion réelle depuis le Deck AVANT de toucher à la cascade. Restent à écrire : **#T381** (canal raisonnement/outils sur le pipeline complet — fichier chaud `core/engine.py`, PAS délégable) et **#T354** (retrait d'Ollama, toujours bloqué). |
| Décisions Axel | [DECISIONS.md](DECISIONS.md) · #T315 #T335 #T326 #T327 #T223 #T260 |

## 🔄 Décisions en attente (campagne 12-13/08)

- ⏳ #T326 — config — **P3** — **DÉCISION AXEL** — `MOTEUR_API_KEY` 29 car. local vs 64 prod → 401 croisés. Unifier ou documenter. — Claude —
- ⏳ #T327 — déploiement — **P3** — **DÉCISION AXEL** — second moteur `vromvrom-engine` :8002 sur le Deck depuis le 03/07 (daemon 10 min, 2 anomalies/cycle). Arrêter ou assumer. — Claude —
- ⏳ #T315 — hitl — **P2** — **DÉCISION AXEL** — HITL dès `medium` (« écrire/créer/exécuter/terminal ») : 3/8 demandes campagne mortes, dont un CSV et un `ls` de `docs/`. — **Axel** —
- ⏳ #T335 — routage — **P3** — **DÉCISION AXEL** — CLI dans le tier `fort` ? Un appel `claude-opus-4-8` = 289 966 tokens in / $0,6665 = 100 % du coût campagne. En prod les binaires sont absents (#T331) donc ça échoue au lieu de coûter. — **Axel** — suite #T314 —

## IHM v2 (câblage, composants déjà mergés)

- 🧪 #T291 — ihm-v2 — **P2** — **CÂBLÉ le 17/08 au soir**, et l'énoncé était incomplet. `ThinkingBlock` + `ToolCallDisplay` sont montés dans `Chat.tsx`, alimentés par `chatStore` (`thinkingText`/`thinkingStreaming`/`thinkingDurationMs`/`toolCalls`) depuis les événements livrés par #T346. Trois précisions issues du travail : (a) **un canal d'étapes était mort** — `chat.ts:73` n'écoutait que `orchestration_step`, un nom que **rien n'émet côté Python** (vérifié par grep sur tout le dépôt) alors que le backend envoie `agent_started`/`agent_completed`/`phase_changed` (`core/engine.py:370,394,1127`) : les badges d'étapes de la vue Chat **et** du Prompt Studio ne se sont jamais affichés ; (b) `tool_result` ne reporte pas les `args`, d'où un type `ToolCallResult` distinct pour que la fusion n'efface pas ceux de l'annonce ; (c) le type `ToolCallData` descend dans `state/chatStore` (source unique), le composant l'importe. `tsc -b` et `vite build` au vert. — Claude —
- ⏳ #T381 — archi/ihm-v2 — **P2** — **Le raisonnement et les outils n'atteignent pas le pipeline complet.** #T346 émet `thinking`/`thinking_done`/`tool_call`/`tool_result`, mais **uniquement depuis `stream_discussion_fast_path_sse`**, c'est-à-dire la branche `source.mode = "chat"` (`api/routes/streaming.py:113,135`). Or la vue Chat envoie `mode: "default"` (`Chat.tsx:195`, idem `PromptStudio.tsx:106`) et part donc dans `run_full_pipeline`, dont les seuls événements sont `agent_started`/`agent_completed`/`phase_changed`/`plan_waiting_approval`… — aucun canal de raisonnement, aucun appel d'outil, alors que c'est l'Executor qui en fait le plus. Les blocs câblés par #T291 restent donc éteints dans l'usage réel de l'IHM. Plomber le canal depuis `Engine.on_event` jusqu'au SSE. ⚠️ Fichier chaud `core/engine.py` → PR dédiée (PLAN.md). — Claude — débloque l'usage réel de #T291 —
- ⏳ #T305 — ihm-v2 — **P3** — **Créer** `TaskKanbanBoard` (il n'existe pas — ce n'était pas un câblage). **Requalifié et spécifié le 17/08** : périmètre et maquette dans `docs/specs_ihm_v2/T305_TaskKanbanBoard_Spec.md` (#T305-a, PR #337). Prêt à planifier. — Claude —

## Chantiers code

> **Tâches récupérées du board divergent, statut re-vérifié le 18/08 contre `origin/master`.**
> Mergées et vérifiées présentes dans le code (donc **closes**, ne pas les relancer) : **#T374**
> (contrat plancher, `fix/contrat-plancher-jugement-17-08`), **#T375** (backoff de la sonde,
> `agent/backoff-sonde-modeles`), **#T376** (hygiène du journal, `fix/hygiene-journal-tls-gemini`),
> **#T377**+**#T380** (watchdog configurable, PR #347), **#T378** (zone grise du catalogue,
> `fix/zone-grise-catalogue-routage`), **#T379** (clôture de session, PR #344), **#T388**+**#T389**
> (`docs/architecture/carte_decision_vocale.md` et `tests/unit/test_ordre_cascade_vocale.py`,
> tous deux présents). ➜ voir archive.

- ✅ #T370 — vocal — **P1** — **DOUBLON DE #T383, aucun travail à lancer.** Les 8 marqueurs signalés morts faute d'accents (`reunion`, `evenement`, `je commence a travailler`, `je finis a quelle heure`, `je termine a quelle heure`, `je suis en conge`, `quoi de prevu`, `de prevu`) sont **tous** présents dans les deux orthographes (PR #348, mergée le 18/08) (`core/vocal_host.py:47,72-80`, vérifié le 18/08). Se clôt avec #T383. — voir archive —
- 🧪 #T371 — domotique — **P1** — **CORRIGÉ le 18/08, PR #350.** Reproduit sur le code de `master` avant d'écrire : « température de la buanderie » **et** « température extérieure » renvoyaient toutes deux le capteur du **salon** — valeur exacte, donc indiscernable d'une bonne réponse. Trois causes distinctes : (a) le repli `_TEMPERATURE_DEFAULT_ROOM` s'appliquait même quand la phrase nommait un lieu ; (b) l'étage d'état passe **avant** l'étage météo dans la cascade, donc #T367 n'était jamais atteint pour l'extérieur — et une fois relâché, `ha_weather_query` ne le reconnaissait pas non plus (marqueurs ajoutés dans les **deux** orthographes, ce module conservant les accents) ; (c) **régression de #T364** : « la température de la chambre » ne répondait plus RIEN alors que `sensor.bedroom_temperature` est vivant (**24,49 °C** relevé sur le HA de prod le 18/08) — la réponse avait été supprimée au lieu que le capteur soit branché. Le test de #T364 qui figeait ce comportement dégradé est actualisé, pas affaibli. 11 tests neufs ; suite complète **1869 passés, 0 échec**. — Claude —
- ⏳ #T372 — domotique — **P2** — **DÉCISION AXEL puis code — l'état du volet vient d'un miroir, pas de l'appareil.** « est-ce que le volet est ouvert ? » → « Le volet est partiellement ouvert. » Le moteur lit `input_text.blind_state` (`services/ha_state_query.py`), qui vaut « Partiel » — la réponse est fidèle à sa source. Mais l'entité matérielle `cover.living_room_blind` est à `unknown` (volet sans capteur de position, cf. #T385). **Les deux divergent, et l'utilisateur reçoit une position affirmative alors que l'appareil n'en rapporte aucune.** À trancher avant de coder : le miroir fait-il foi (il sait ce qui a été commandé) ou l'entité (elle sait ce qui est) ? Et que répondre quand ils se contredisent ? — **Axel** puis Claude —
- 🧪 #T368 — tests — **P2** — **LIVRÉ le 18/08 à 07:46 (PR #349)**, après deux échecs (moteur : faux succès sans fichier — l'incident qui a produit #T374 ; puis réassignation). ⚠️ **Il contredit #T371 sur une ligne, et c'est #T368 qui a tort** : le banc fige « il fait combien dans la chambre ? » → aucune lecture d'état, au motif que la chambre serait une pièce **sans capteur**. Or `sensor.bedroom_temperature` répond **24,49 °C** sur le HA de prod (relevé le 18/08). Vérifié en fusionnant les deux branches en local : **1 cas sur 36 échoue**, `test_chaque_phrase_prend_la_bonne_decision[#T364 | il fait combien dans la chambre ?]`. **Ordre de merge : #349 d'abord, puis #350 rebasée** — la ligne du banc passe alors à `"temperature"` attendu. Ne PAS merger #350 en premier : le banc tomberait rouge sur `master` et la CI est morte pour le dire (#T382). — **DeepSeek** —


- 🧪 #T385 — domotique — **P0** — **CORRIGÉ le 17/08, décision Axel, avant déploiement.** #T356 allait casser la commande vocale la plus utilisée : le code mergé traitait `unknown` **exactement comme** `unavailable` (`services/execute_service.py`), et `_entite_physique_a_verifier` redirige le script de volet vers `cover.living_room_blind`. Or ce volet — **le seul de l'installation** — est en `unknown` **et fonctionne** (mesure HA du 17/08 restée sur `origin/docs/board-lot-13-deepseek`, `358ebde` ; script relancé avec succès à 13:53) : c'est un volet **sans capteur de position**, `unknown` est son état normal. Sans ce correctif, chaque commande de volet aurait répondu « le volet de la serre ne répond pas » **pendant que le volet bouge**. Correctif volontairement **étroit** : l'exception ne couvre que le domaine `cover` **sans** attribut `current_position` ; `unavailable` bloque toujours, une `light` en `unknown` bloque toujours, et un volet qui SAIT rendre sa position reste suspect en `unknown`. 6 tests, dont le chemin réel via le script. ⚠️ **À re-vérifier en prod après déploiement** : rejouer une commande de volet et confirmer qu'elle n'est pas refusée. — Claude —
- 🧪 #T383 — vocal — **P1** — **CORRIGÉ le 17/08 à 23h55, avant déploiement.** #T365 était **à moitié mort en prod** : `_normalize_prompt` (`core/vocal_host.py:131-132`) conserve les accents — sa classe de caractères gardés contient explicitement `àâäéèêëïîôùûüç` — or les marqueurs du champ horaire avaient été écrits **sans** accent. Mesuré : « je commence à travailler », « je finis à quelle heure », « je termine à quelle heure », « je suis en congé », « quoi de prévu », « de prévu » ⇒ **zéro marqueur touché**, retour au chat. Le défaut a franchi le merge parce que les tests de #T365 écrivent leurs phrases sans accent (`tests/unit/test_vocal_host.py:331,334`) : ils validaient une entrée que le STT ne produit pas. Correctif : les **deux** orthographes listées, comme le fait déjà `_HA_STATE_MARKERS` (« c est allume » / « c'est allumé »). 12 tests neufs, dont un qui fige la cause racine (si la normalisation se met un jour à dépouiller les accents, il tombe au lieu de laisser pourrir les doublons). — Claude —
- ⏳ #T384-a — hygiène — **P3** — **Supprimer `origin/fix/dag-watchdog-configurable`** (décision Axel 17/08) : doublon, c'est la variante `feat/` qui a été mergée en PR #347. **Non faisable depuis la session cloud** — le proxy git refuse la suppression de ref (`send-pack: unexpected disconnect`, 4 tentatives) et l'API GitHub exposée n'a pas d'outil de suppression de branche. À faire depuis le PC : `git push origin --delete fix/dag-watchdog-configurable`, ou en un clic dans la liste des branches GitHub. — Axel —
- ⏳ #T384 — hygiène — **P3** — **Travail resté sur `origin/backup/wip-soir-2026-08-17`, à trier.** Le code du soir est bien mergé (vérifié fichier par fichier), mais 7 fichiers ne sont nulle part ailleurs : `scripts/verify_workspace.py` (300 l.), `scripts/worktree_manager.py` (120 l.), `scripts/review_changes.py`, `scripts/test_dag_parallelism.py`, `scripts/test_workflow_deepseek.py`, `docs/travail/AUDIT_IHM_V2.md`, et `api/routes/context_fix.py` (**doublon de brouillon, à jeter**). Le banc `tests/unit/test_regression_prod_17_08.py` de cette branche a servi à trouver #T383 mais **sa détection calendrier est fausse** (comparaison sur `phrase.lower()` sans passer par le classifieur) : à réécrire sur `classify_vocal_intent` avant de le reprendre. ⚠️ Sa copie de `test_bulk_status_libere_sonde.py` est **périmée** — `master` porte la version corrigée (`da5a0b0`). — Claude —
- ⏳ #T382 — ci — **P1** — **La CI ne tourne plus et personne ne l'a vu.** Les 10 derniers runs de `ci.yml` échouent, `master` compris ; les 22 PR du 17/08 au soir ont donc été mergées **sans aucune preuve CI**. Diagnostic posé le 17/08 à 23h45 : run de **4 s** pour 3 jobs, **logs en 404**, et le job Ruff échoue alors que ses étapes sont toutes `continue-on-error` → l'échec est **avant le `checkout`**, ce n'est pas du code. **Cause confirmée par Axel : limite d'utilisation Actions atteinte.** Reste à trancher : relever le quota, ou basculer la CI sur un runner auto-hébergé (le Deck tourne déjà 24/7). **Action Axel.** Garde-fou en attendant : exécuter la suite en local avant tout merge (`pytest tests/unit` + `npm run build`), et ne pas lire un ✅ GitHub comme une preuve. — Axel/Claude —

- ⏳ #T353 — runtime — **P2** — **DÉCISION AXEL 17/08 : abandon d'Ollama au profit d'AJEAN** (gestionnaire llama.cpp d'Axel, **pas** Jan/jan.ai), sur le PC ET sur le Deck. Câbler deux providers (`ajean_pc` 192.168.1.x, `ajean_deck` 127.0.0.1) et les mettre en tête du tier local. **MAJ 17/08 soir — AJEAN EXISTE ET TOURNE** : `C:\Users\axell\ajean\llamacpp-vulkan\llama-server.exe` (PID 38816, build llama.cpp `b10451`), modèle `Qwen2.5-14B-Instruct-1M-Q4_K_M.gguf`, `-ngl 999 -c 32768 -ctk/-ctv q8_0`, lancé au logon par `C:\ProgramData\ajean\bin\ajean.exe start`. **Complétion réelle obtenue** sur `POST /v1/chat/completions` (pas seulement `/v1/models`), avec bloc `usage` réel. ⚠️ **Bloqué par un seul réglage : `--host 127.0.0.1`** → depuis le Deck, `192.168.1.x:8080` est injoignable (testé). **Action Axel : faire écouter AJEAN sur `0.0.0.0` + ouvrir le pare-feu Windows sur le LAN.** Le câblage déclaratif est délégué sans attendre → **#T353-a** (16ᵉ série). Trace périmée : le Deck a bien un `127.0.0.1:1337` qui répond `Forbidden`, service non identifié, sans rapport avec AJEAN. Prérequis Axel : serveur d'API Jan activé, écoute sur `0.0.0.0`, pare-feu Windows ouvert sur le LAN — mêmes prérequis que ceux déjà documentés pour `ollama_pc` (`core/openai_compat_provider.py:697-700`). **Bonne nouvelle pour #T337** : `_est_hote_local` (`core/llm_gateway.py:820-832`) accepte déjà toute IP privée, donc Jan sera reconnu local sans modification — **à condition de le déclarer par IP et jamais par nom DNS** (l. 830-831 : un nom d'hôte est traité comme externe). — Claude puis DeepSeek — bloque #T354 —
- ⏳ #T354 — runtime — **P2** — **Retrait complet d'Ollama** (provider `ollama_local` + `ollama_pc`, descripteur `OllamaLocalPlugin` `core/llm/builtin_providers.py:327`, entrées `core/openai_compat_provider.py:689-706`, cascade `services/pipeline_service.py:82`, modèles du catalogue). ⚠️ **NE PAS FAIRE AVANT #T353 vérifié** : mesuré le 17/08, `127.0.0.1:11434` répond **HTTP 200 sur le Deck** avec 3 modèles (`qwen2.5-coder:1.5b`, `phi3:mini`, `gemma2:2b`) — c'est le **seul runtime local qui répond en production aujourd'hui**. Le retirer avant que Jan ne réponde ouvrirait une fenêtre sans aucun local : `privacy_level=local_only` (#T337) deviendrait fail-closed permanent, et le fast-path vocal perdrait son premier maillon. — DeepSeek — dépend #T353 —
- ⏳ #T282 — cli — **P2** — Trust workspace Claude CLI : `hasTrustDialogAccepted: true` dans `~\.claude.json`. — Antigravity —
- ⏳ #T179 — scraper — **P2** — `billing_scraper.js` Anthropic + Gemini abo. Valider `--headless=false`. — Claude —
- 🧊 #T183 — catalogue — **P3** — 4 modèles Cohere payants non câblés (gelé Axel). — Claude —
- ⏳ #T184 — billing — **P3** — Cerebras jamais testé en direct (Chrome + API solde). **NB** : les 2 clés répondaient 403 le 16/08. — Claude —
- ⏳ #T144 — infra — **P3** — CRLF dans `.env` du Deck. — Axel/Claude —
- ⏳ #T191 — mémoire — **P3** — Agent d'expansion sémantique de la mémoire active. — Claude —

## À voir (produit, pas le hot-path)

- ⏳ #T336 — auth — **P3** — Multi-tenant + API keys RBAC. — Claude —
- ⏳ #T338 — ihm-v2 — **P3** — Dashboard multi-bâtiments. — Claude —
- ⏳ #T339 — b2b — **P3** — Pack « VromVrom Box » hôtellerie/serres. — Axel —
