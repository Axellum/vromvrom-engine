# ─────────────────────────────────────────────────────────────────────────
# Moteur Multi-Agents V12 — Dépendances de développement / test
# Installer avec : pip install -r requirements.txt -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────

# ── Tests ──
pytest>=8.0
pytest-asyncio>=1.3.0      # mode asyncio_mode=auto (pyproject.toml)
pytest-cov>=5.0            # couverture (--cov configuré dans pyproject.toml)
paramiko>=3.0             # requis par test_failover (failover_manager SSH)

# ── Qualité / lint ──
ruff>=0.6.0                # linter + isort (config dans pyproject.toml)
mypy>=1.10.0               # typage statique (config dans pyproject.toml)
