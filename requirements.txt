# ─────────────────────────────────────────────────────────────────────────
# Moteur Multi-Agents V12 — Dépendances runtime
# Versions plancher alignées sur l'environnement validé (2026-06-16).
# Dépendances dev/test : voir requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────

# ── Core / API ──
fastapi>=0.141.1
uvicorn>=0.52.1
starlette>=1.3.1
pydantic>=2.13.4
jinja2>=3.1.6
python-dotenv>=1.2.2

# ── Clients HTTP (sync + async) ──
requests>=2.34.2
httpx>=0.28.1
aiohttp>=3.14.3

# ── Persistance ──
aiosqlite>=0.22.1          # accès SQLite async (DAG runner)

# ── MCP (serveurs Tab5-Engine, Workspace) ──
# Plafonné à <2.0.0 : mcp 2.0.0 (28/07/2026) supprime mcp.server.fastmcp
# et casse la CI de toutes les PR dependabot en aval. Migration vers l'API v2
# à traiter séparément.
mcp>=1.27.0,<2.0.0

# ── Mémoire vectorielle / RAG ──
chromadb>=1.5.9
numpy>=2.5.1
scikit-learn>=1.9.0        # ML Router (couche 2 du routage) — installé sur le Deck 2026-06-17
onnxruntime>=1.28.0        # backend embeddings ChromaDB (all-MiniLM-L6-v2)

# ── Utilitaires ──
cachetools>=7.1.6
jsonschema>=4.26.0
PyYAML>=6.0.3
paho-mqtt>=2.1.0,<3.0      # failover_manager (MQTT)
filelock>=3.32.0             # verrous sur fichiers partagés (config.json, BDD)
cryptography>=49.0.0         # chiffrement au repos de google_token.json (Fernet, MCP_TOKEN_KEY)

# ─────────────────────────────────────────────────────────────────────────
# Dépendances OPTIONNELLES (le moteur dégrade gracieusement si absentes)
# Décommenter selon les fonctionnalités souhaitées.
# ─────────────────────────────────────────────────────────────────────────
# langfuse>=2.60,<3.0      # tracing/observabilité (core/langfuse_bridge.py)
# opentelemetry-api>=1.20  # Tracing OTLP — API légère (no-op si SDK absent)
# opentelemetry-sdk>=1.20  # SDK complet (requis pour exporter vers Langfuse/Phoenix)
# opentelemetry-exporter-otlp-proto-http>=1.20  # Export OTLP/HTTP
# anthropic>=0.40.0        # SDK Anthropic direct (sinon accès via Claude CLI)
google-cloud-logging
